# 局部重绘性能与正确性优化——合并说明（2026-08-13）

## 本轮目的

在不降低显示质量、不隐藏蒙版、不限制高刷新率的前提下，解决局部重绘进入后出现的交互卡顿和显示错误：

- 左键绘制、右键擦除必须即时可见，停笔不能卡住主线程。
- 蒙版在旋转、移动、缩放以及工具切换时不得消失或闪烁。
- 实时笔迹与停笔后的结果必须一致，不能出现先穿透模型、随后才按法线收缩的过程。
- 大量笔画不再按历史笔划逐层渲染；使用可复用缓冲累计结果。
- 局部重绘状态下的高速滚轮缩放应接近显示器刷新率；高刷显示器不锁 60 FPS。
- 右上角性能窗及“开始/结束人工录制”按钮保留，用于复测落笔、停笔、滚轮、旋转和长任务。

## 已提交成果

主提交：`326193a 基本上修复局部重绘问题`

核心改动集中在：

- `apps/web/src/engine/viewport/ViewportCanvas.tsx`

已实现：

1. 使用固定 UV 累计缓冲保存历史蒙版，实时只渲染当前投影笔迹，避免笔画越多、绘制层越多。
2. 绘制过程中只合并当前帧需要的输入；停笔不再同步编码整张 PNG，也不做全画布像素扫描。
3. 蒙版反选改为 GPU uniform，避免 CPU 读回和重新生成纹理。
4. 原始模型材质内直接混合蒙版，减少一套完整模型覆盖层的三角形和 Draw Call；编译完成前保留覆盖层兜底，避免闪白或蒙版消失。
5. 相机移动时不重复归档蒙版、复制画布或重建投影器；当前投影固定在模型空间，视觉不会随相机移动消失。
6. 实时笔迹从第一帧执行模型法线朝向约束，避免正反面同时出现、约一秒后才收缩。
7. 实时层和累计层统一使用参考图采样的红橙色 `#ac2f0d`，避免落笔偏粉、停笔后换色。
8. 恢复并保留右上角轻量性能窗、性能实验室和人工录制按钮。

## 当前未提交的交互保护改动

文件：

- `apps/web/src/engine/viewport/BlenderOrbitControls.ts`
- `apps/web/src/engine/viewport/ViewportCanvas.tsx`
- `apps/web/src/engine/viewport/viewportInteractionState.ts`

目的：精密滚轮或触控板在一个显示周期内会连续产生多个 `wheel` 事件。旧逻辑每个原始事件都立即执行相机 `lookAt` 和矩阵更新；局部重绘材质启用后，这种事件风暴会明显放大 CPU 峰值。

新逻辑：

- 累加一个显示帧内的全部滚轮增量。
- 每个原生 `requestAnimationFrame` 只更新一次相机。
- 不丢缩放距离，不固定为 60 FPS；60/120/144Hz 屏幕均按自身刷新周期更新。
- `dispose` 时取消尚未执行的帧并清空累计量。
- 滚轮爆发只在开始和安静后通知后台 Worker 调整预算，原始滚轮事件只更新交互时间戳。
- 同一相机、同一加减模式下的短笔共享当前投影缓冲，不再每个点归档整张 UV 蒙版。
- 蒙版由空变为非空时只广播一次 React/store 状态，后续停笔不重复刷新无关面板。
- 材质已预热后不再每笔搜索覆盖网格，也不再每笔遍历/重绑整台模型。
- `perfLab=1` 不再每次停笔同步输出对象到控制台；仅显式 `paintVerbose=1` 才输出详细笔画日志。
- 点按停笔不再无条件执行 `getBoundingClientRect()`；仅停笔位置确实需要补采样时读取布局。

以上改动已通过 TypeScript 类型检查，但尚未提交，合并者应独立提交并按下方流程复测。

## 当前验证结果

- 冷刷新后页面正常，无编辑器错误覆盖层。
- 蒙版左键绘制恢复正常；颜色与参考图一致。
- 实时笔迹首帧法线约束生效，没有再次观察到正反面同时穿透。
- 稳定静止状态：`60 FPS / P95 16.8ms`。
- 40 次独立短笔自动复测中，落笔/停笔到下一帧 P95 分别约 `14.1 / 14.5ms`；自动化 CUA 点击本身会产生外部长任务，因此不把该轮平均 FPS 当作产品验收结果。
- 当前 10.5 秒高速滚轮复测：局部重绘已开启且已有蒙版，618 次大幅度交替滚动，区间平均 `59.6 FPS`、P95 `16.8ms`、掉帧 1 次。
- 滚轮复测结束后视口恢复 `60 FPS / P95 16.8ms`，性能窗与人工录制按钮均保留。

## 尚未完成及下一步

1. **仍需真实鼠标最终验收。** CUA 能覆盖持续时间和事件数量，但无法完全复刻实体滚轮驱动与浏览器 IPC；合并前按同一流程手测一轮。
2. **极端短点仍需观察最大帧。** 热路径已移除整张 UV 归档、重复 React 广播、逐笔模型重绑、同步调试日志和无条件布局读取；若实体鼠标仍有峰值，应以人工录制 JSON 继续定位，不能重新把同步合成塞回停笔。
3. 上述交互保护尚未写入主提交，合并时不要误标为 `326193a` 已包含。

## 合并与复测建议

1. 先合并 `326193a`。
2. 将 `BlenderOrbitControls.ts` 的滚轮帧合并改动作为独立提交，便于单独回退和 A/B 对照。
3. 执行：`corepack pnpm --filter @liclick/web typecheck`。
4. 打开带 `?perfLab=1` 的贴图页面，确认右上角性能窗和人工录制按钮存在。
5. 开始人工录制后执行：
   - 至少 20 笔大画笔，每笔停 0.5 秒；
   - 覆盖模型表面 90% 以上，再用右键擦除；
   - 交替执行快速旋转、移动、图层开关；
   - 在局部重绘仍开启且已有蒙版时，连续大幅滚轮缩放至少 10 秒。
6. 验收重点：蒙版不消失、不穿透、不换色；落笔和停笔无明显长帧；60Hz 环境维持 60 FPS 附近，高刷环境不锁帧。

## 合并边界

本轮只涉及局部重绘显示/输入/性能路径和相机滚轮事件合并，不应顺带修改投影图层业务、烘焙流程、模型数据或生成接口。
