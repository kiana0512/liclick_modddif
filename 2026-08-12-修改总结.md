# Li3D 本地版今日有效优化总结

日期：2026-08-12
项目目录：`D:\Li3D-1`

## 一、前端入口与导航优化

### 贴图绘制入口

- 首页点击“贴图绘制”后，先进入贴图项目列表。
- 用户在项目列表点击“新建项目”后，再进入贴图绘制界面。
- 首页入口提示文案调整为“查看贴图项目”。

### Li3D 品牌区交互

- 取消点击或双击 `Li3D` 文字触发页面跳转的交互。
- Logo 在支持返回的页面中，鼠标悬停或键盘聚焦时切换为左箭头。
- 编辑器、工作流页面和项目列表统一通过明确的路由回调返回。
- 移除项目页中重复的“返回功能首页”按钮，简化页面结构。

关键文件：

- `apps/web/src/App.tsx`
- `apps/web/src/components/common/BrandMark.tsx`
- `apps/web/src/layouts/EditorShell.tsx`
- `apps/web/src/features/workflow/WorkflowShell.tsx`
- `apps/web/src/routes/HomePage.tsx`
- `apps/web/src/routes/ProjectsPage.tsx`

## 二、本地运行时稳定性优化

- 冷启动连续 3 次无法连接后才判断本地组件不可用，减少瞬时网络波动造成的误判。
- 健康检查请求超时由 1.6 秒提高到 4 秒。
- 多个并发健康检查复用同一个请求，避免轮询、窗口聚焦和手动检测产生重复请求。
- 首次成功连接后记录当前编辑器会话已准入。
- 本地组件短暂无响应时保留编辑器和当前生成任务，只显示顶部自动重连提示。
- 断线后使用指数退避自动重连，最长间隔为 5 秒。
- 增加会话版本控制，避免离开页面后旧的异步检测结果写入新页面。
- 保持原有生成任务提交并发策略不变。

新增文件：

- `apps/web/src/hooks/localTextureRuntimePolicy.ts`
- `apps/web/scripts/test-local-runtime-health.mjs`

关键修改文件：

- `apps/web/src/hooks/useLocalTextureRuntime.ts`
- `apps/web/src/services/localTextureRuntimeClient.ts`
- `apps/web/src/components/runtime/TextureRuntimeGate.tsx`
- `apps/web/src/App.tsx`

## 三、局部重绘优化

### 平面视图作为生成基准

- 局部重绘发送给生成服务的原图统一使用 `flat-target` 平面 BaseColor。
- 原图不包含 PBR 灯光、曝光和色调映射，遮罩与原图保持相同相机、尺寸和构图。
- 返回图片按照平面 BaseColor 写回模型。
- 在 PBR 视图下，由预览着色器统一叠加当前环境光、主光和曝光参数。
- 平面视图中的局部重绘颜色与生成返回图保持一致，PBR 与平面视图之间的颜色转换更加稳定。

关键文件：

- `apps/web/src/components/panels/GeneratePanel.tsx`
- `apps/web/src/engine/capture/captureCurrentView.ts`
- `apps/web/src/engine/capture/captureTypes.ts`
- `apps/web/src/engine/viewport/previewLighting.ts`
- `apps/web/src/engine/viewport/SceneRoot.tsx`
- `apps/web/src/engine/projection/ProjectedLayerMaterial.ts`

### 多个局部重绘图层

- 每次新的远端局部重绘结果使用独立目标图层。
- 同一次生成结果的连续笔画继续累积在同一图层。
- 新的生成结果使用新的目标图层和遮罩，不再覆盖历史重绘层。
- 旧项目中的实时预览地址会恢复为对应图层自己的持久化生成图片。
- 删除局部重绘层时同步处理其目标层和预览层，不影响其他局部重绘结果。

关键文件：

- `apps/web/src/engine/localRepaint/sessionLayer.ts`
- `apps/web/src/components/panels/LayersPanel.tsx`
- `apps/web/src/routes/EditorPage.tsx`
- `apps/web/src/stores/layerStore.ts`
- `apps/web/src/stores/sceneStore.ts`

### 历史图层与实时预览切换

- 历史持久图层与当前实时 GPU 覆盖层分开管理。
- 切换生成结果时先卸载实时覆盖层，再释放预览所有权，最后恢复持久图层显示。
- 避免同一位置同时渲染两张共面贴图，提升多次重绘后的显示稳定性。
- 当前编辑层继续使用快速 GPU 预览，历史层保持持久显示。

关键文件：

- `apps/web/src/engine/viewport/ViewportCanvas.tsx`
- `apps/web/src/engine/viewport/SceneRoot.tsx`
- `apps/web/src/engine/viewport/localRepaintGpuOverlaySync.ts`
- `apps/web/src/engine/projection/ProjectedLayerPreviewCompositor.ts`

### 特殊位置投影可见性

- 为局部重绘投影增加 `surface-locked-v1` 表面锁定策略。
- 使用投影视角的深度和法线信息限制贴图显示范围。
- 增加最小正面夹角和边缘羽化，减少贴图投射到背面、内壁或相邻几何体。
- GPU 预览与 UV 烘焙共用投影可见性字段，使预览和烘焙结果保持一致。

关键文件：

- `apps/web/src/types/layer.ts`
- `apps/web/src/engine/projection/projectionTypes.ts`
- `apps/web/src/engine/projection/createRuntimeProjectionDepth.ts`
- `apps/web/src/engine/projection/ProjectedLayerMaterial.ts`
- `apps/web/src/engine/bake/gpuUvBakeRenderer.ts`

## 四、烘焙与 BaseColor 合并优化

### 自动烘焙

- “自动烘焙”文案调整为“自动烘焙 BaseColor”。
- 烘焙前检查现有合并 UV 是否与当前可见图层一致。
- 合并 UV 后新增局部重绘、投影层或内容识别修补层时，先完成一次新的图层合并，再进入烘焙。
- 当前合并 UV 可直接复用时跳过重复计算。
- 旧版本项目中的有效局部重绘补丁可以重新合入最新 BaseColor。

### 平面与 PBR 颜色合成

- 新增 UV 合成版本 `UV_MERGE_COMPOSITION_VERSION = 4`。
- 增加 `renderedColorMaskUrl`，记录已经包含显示颜色的 UV 像素。
- 已包含显示颜色的局部重绘像素保持原值，避免重复应用曝光和灯光。
- 普通 BaseColor 像素按照当前 PBR 预览光照进行一致化处理。
- 项目保存、恢复和资源修复流程支持渲染颜色遮罩资源。

新增文件：

- `apps/web/src/engine/bake/projectedOverlayComposition.ts`

关键修改文件：

- `apps/web/src/features/workflow/selectBakeBaseColor.ts`
- `apps/web/src/engine/bake/bakeProjectedLayerToTexture.ts`
- `apps/web/src/engine/bake/gpuUvBakeRenderer.ts`
- `apps/web/src/engine/bake/qualityBlendWorker.ts`
- `apps/web/src/engine/layers/mergeUvComposition.ts`
- `apps/server/src/services/projectFileService.ts`

## 五、BaseColor 与模型导出优化

- BaseColor 下载改为输出当前可见图层的完整合成结果。
- 导出时包含尚未完成后台持久化、但已经显示在视口中的局部重绘快照。
- 调整导出合成顺序：普通 UV BaseColor 位于底层，投影烘焙结果位于中间，局部重绘补丁位于最上层。
- 单独 BaseColor PNG 下载与模型导出共用可见图层合成逻辑。
- 导出的 GLB、GLTF 和 BaseColor PNG 可以保留当前局部重绘效果。

关键文件：

- `apps/web/src/engine/export/exportTexture.ts`
- `apps/web/src/engine/export/texturedExportUtils.ts`
- `apps/web/src/routes/EditorPage.tsx`

## 六、内容识别修补多图层优化

- 单个内容识别修补层直接使用驻留纹理。
- 多个可见修补层按照图层顺序合成为一个 underlay。
- UV 合成队列按 `ownerKey` 隔离，不同对象和不同用途的合成任务互不取消。
- 多张纹理并行解码，并预热常用 UV 图层。
- 新纹理准备期间保留上一张已完成纹理及其透明度。
- 纹理与透明度原子切换，避免纹理尚未上传时提前显示占位内容。
- 图层内容变化时递增 `contentRevision`，使缓存和材质及时刷新。
- 增加内容识别修补层数量、纹理状态、透明度和 UV 合成队列的运行时诊断数据。

关键文件：

- `apps/web/src/engine/viewport/SceneRoot.tsx`
- `apps/web/src/engine/projection/ProjectedLayerMaterial.ts`
- `apps/web/src/engine/layers/uvLayerCompositeWorker.ts`
- `apps/web/src/engine/layers/mergeUvComposition.ts`
- `apps/web/src/stores/layerStore.ts`

## 七、已通过验证

- Web TypeScript 类型检查。
- 本地运行时健康检查与编辑器门禁回归测试。
- 烘焙 BaseColor 选择回归测试。
- UV 合成队列背压回归测试。
- 生成任务轮询与计时回归测试。
- Git 差异格式检查。

## 八、本地启动命令

在 `D:\Li3D-1` 目录执行：

```powershell
corepack pnpm dev
```
