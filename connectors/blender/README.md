# Blender Connector Placeholder

The first phase does not implement a real Blender connector.

Long-term goals:

- Import and export GLB assets.
- Apply generated and baked texture maps.
- Sync material slot names and selected object metadata.
- Use `@liclick/connector-protocol` for stable message contracts.
